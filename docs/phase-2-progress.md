# Phase 2 — Chat Experience: Progress

This file tracks Phase 2 implementation progress. Updated as each task ships.

**Plan:** `docs/superpowers/plans/2026-05-02-matron-ios-phase-2-chat-experience.md`

**Branch:** `phase-2-chat-experience`

## Status

Started 2026-05-02. Phase 1 shipped; this branch builds on top.

## Tasks (will be checked off as completed)

See the plan for the canonical task list. Progress updates land here per push so a PR reviewer can scan a single file for context.

- [x] **Task 1** — MarkdownUI + swift-snapshot-testing wired into `MatronShared/Package.swift`; added `DesignSystemSnapshotTests` test target. `MatronDesignSystem` was already declared in `project.yml` from Phase 1, so no project-yml churn.
- [x] **Task 2** — `MarkdownText` primitive shipped with `Theme.matron` (system font, monospaced inline code, copy-button code blocks, accent-coloured underlined links). 4 snapshot tests × 3 macOS variants = 12 baselines recorded. Plan deviation: `swift-snapshot-testing` only ships a SwiftUI-aware `.image` strategy on iOS/tvOS, so on macOS the `assertVariants` helper hosts the SwiftUI view in `NSHostingView` and snapshots that. iOS variants will be wired up when this suite gets a host xcodebuild scheme.
- [x] **Task 2b** — Promoted private `CodeBlockView` to public `CodeBlock` primitive in its own file; `MarkdownText`'s `.codeBlock` theme hook now delegates to it. 2 snapshot tests × 3 macOS variants = 6 new baselines. Existing MarkdownText baselines unchanged (byte-identical render).
- [x] **Task 3** — `AttachmentImage` (image-with-placeholder + caption) and `AttachmentFile` (icon + filename + ByteCountFormatter size) primitives shipped. 3 snapshot tests × 3 macOS variants = 9 new baselines. Per the plan note, `AttachmentFile`'s background uses `Color.matronCodeBg` (cross-platform alias from MarkdownText) instead of iOS-only `Color(.systemGray6)`.
- [x] **Task 4** — `TimelineItem` DTO shipped with `Kind` (text/image/file/stateChange/unknown) and `SendState` (sent/sending/failed) enums. 5 unit tests cover equality, kind disjointness, stable id, default sendState, and failure-reason equality.
- [x] **Task 5** — `TimelineService` protocol + `TimelineServiceLive` (SDK-backed) + `FakeTimelineService` shipped. 5 fake-driven tests cover snapshot streaming, sendText/Image/File call-recording, paginate/markAsRead. Plan deviations to match SDK v26 (26.04.01): `messageEventContentFromMarkdown(md:)` is a single-arg call (no html); `sendImage`/`sendFile` take `UploadParameters` + `thumbnailSource:` (not `url:thumbnailUrl:`); `paginateBackwards(numEvents:)` (not `opts:`); `markAsRead` lives on `Timeline` not `Room`; SDK exports `TimelineListener` (no `Protocol` suffix). The diff listener also handles `.popFront`/`.popBack`/`.truncate` cases the plan omitted.
- [x] **Task 5b** — Timeline diff application is implemented as `TimelineSnapshotListener.onUpdate(diff:)` in `TimelineServiceLive` (Task 5). `SnapshotApplier` is the test-only mirror that runs the same switch statement over a `FakeDiff` enum. 19 tests cover all 11 SDK `TimelineDiff` cases (including the v26-only `popFront`/`popBack`/`truncate`), the local-echo → remote-event id swap, out-of-bounds clamping, and unknown-event preservation. Also dropped the stale `MatronShared/Sources/DesignSystem/_Module.swift` placeholder.
- [x] **Task 6** — `ChatService.createChat(with:)` extends the protocol; `ChatServiceLive.createChat` builds a `CreateRoomParameters` (encrypted, direct, private preset, single-bot invite) and delegates to `Client.createRoom`. 2 fake-driven tests exercise the protocol surface; existing fakes (`FakeChatService` + `MatronMacTests/LocalFakeChatService`) updated for the new requirement. Plan call site matches v26 (26.04.01) `CreateRoomParameters` exactly.
- [x] **Task 7** — `BotCommand` model + `BotCommandCatalog.claudeBridge` (14 commands) shipped in `MatronModels`. `filter(_:byPrefix:)` is case-insensitive and tolerant of either `/` or `!` prefix. 5 tests cover empty prefix → all, prefix matches, bang prefix, no-match → empty, and uniqueness of triggers in the catalog.
- [x] **Task 8** — `ComposerViewModel` (@Observable @MainActor, no deinit) shipped in `MatronViewModels`. State: `input`, `isSending`, `sendError`, `palettePinnedOpen`. Computed `showPalette` (single-token `/`/`!`) + `filteredCommands`. `send()` trims, clears on success, preserves input + records error on failure. `attachFiles(_:)` is the Mac drag-drop entry point — sends image/file by MIME prefix. 7 tests cover palette show/hide, single-token boundary, command selection, send happy path, empty-input no-op, and send-error preservation.
- [x] **Task 9** — `ComposerView` + `SlashCommandPalette` + `AttachmentPicker` shipped under `Matron/Features/Chat/Composer/`. Plan deviation: the plan deferred `fileImporter` + `PhotosPicker` wiring to Task 11 ("Wired in Task 10 once Live timeline supports sendFile"), but `ComposerViewModel.attachFiles(_:)` and `TimelineService.sendImage`/`sendFile` already exist (Tasks 5 + 8), so the photo picker and file importer route through `attachFiles(_:)` immediately — no dead state. 2 binding tests in `MatronTests/ComposerViewBindingTests.swift` cover input → send wiring and command-list passthrough to the palette.
- [x] **Task 10** — `ChatViewModel` (@Observable @MainActor, no deinit) shipped in `MatronViewModels`. State: `roomID`, `items`, `error`. `start()` returns the observation `Task` so tests await drain deterministically (no sleeps). `stop()` cancels for `View.onDisappear`; `paginateBackward`/`markAsRead`/`refresh` forward to `TimelineService`. 7 tests cover snapshot streaming, deterministic completion (2s bound), paginate/markAsRead invocation, refresh-routes-to-paginate, no-error-on-success, and stop-idempotency. Reused the existing `FakeTimelineService` from `ComposerViewModelTests.swift` instead of duplicating it.
- [x] **Task 11** — `MessageBubble` primitive shipped in `MatronDesignSystem` (public `MessageAuthorStyle` enum + generic `Content`-builder bubble). `TimelineItemView` (iOS) renders text/image/file kinds inside the bubble and renders `.stateChange`/`.unknown` as small inline notices instead. 2 snapshot tests × 3 macOS variants = 6 new baselines. Per the cross-cutting context note, the bubble's filled background uses `Color.matronCodeBg` (cross-platform alias) instead of the plan's iOS-only `Color(.systemGray6)`. Image-attachment caption falls back to a `ByteCountFormatter` size string when no caption is present (more readable than the plan's raw byte count).
- [x] **Task 12** — `ChatView` (iOS screen) shipped at `Matron/Features/Chat/ChatView.swift`. Hosts a `ScrollView` + `LazyVStack` of `TimelineItemView` rows above the existing `ComposerView`. `ScrollViewReader` auto-scrolls to the latest item on `items.count` change; long-press `contextMenu` on text items exposes Copy + ShareLink. `.task` calls `viewModel.start()` + `markAsRead()`; `.onDisappear` calls `viewModel.stop()`. 2 binding tests in `MatronTests/ChatViewBindingTests.swift` cover stream observation through the view-model and title/callback plumbing.
- [x] **Task 12b** — `MediaService` protocol + `MediaServiceLive` (SDK-backed via `Client.getMediaContent`, 64 MB `NSCache` ceiling) shipped. `ChatViewModel` gained a `media: MediaService` init parameter, a `resolvedImages: [URL: Image]` cache, and an `image(for:)` method that coalesces concurrent requests for the same `mxc://` URL. `TimelineItemView` accepts a `resolveImage` closure so `ChatView` can wire `viewModel.image(for:)` to `AttachmentImage`. 5 new tests (3 fake, 2 ViewModel — populate + coalesce). Plan deviations: (a) MediaService lives in the existing `MatronChat` SPM target as `Sources/Chat/MediaService.swift` rather than the plan's separate `Sources/Media/` target — keeps `Package.swift` unchanged and matches the test's `@testable import MatronChat`; (b) added an `inFlightRequests` set so SwiftUI re-renders that re-call `image(for:)` don't fire duplicate fetches.
- [x] **Task 13** — iOS ChatList → ChatView navigation wired via `NavigationLink(value:)` + `.navigationDestination(for: ChatSummary.self)`. The hosting `NavigationStack` lives in `MatronApp` and injects `\.appDependencies` and `\.currentSession` into the environment so the destination column can construct `TimelineService` + `MediaService` + `ComposerViewModel`. `ChatService` protocol grew `refresh()`, `mute(roomID:)`, and `leave(roomID:)`; `ChatServiceLive` implementations call `client.getNotificationSettings().setRoomNotificationMode` for mute and `room.leave() + room.forget()` for leave. `AppDependencies.timelineService(for:roomID:)` factory caches per `(userID, roomID)` so re-navigating to the same room re-uses the SDK timeline handle. 4 new ChatActions tests (one per action + multiple-call counter). Plan deviations: (a) refresh's "force-sync" path in v26 has no `forceSyncOnce` — the live impl awaits `sync.waitUntilReady()` and the consumer re-subscribes to `chatSummaries()`; (b) the `+` toolbar button shows a `NewChatPlaceholder` until Task 14 lands `NewChatSheet`; (c) made `ChatSummary` `Hashable` (was Equatable+Identifiable+Sendable) since `navigationDestination(for:)` requires `Hashable`. Existing `LocalFakeChatService` and `FakeChatService(ForCreate)` fakes updated for the new protocol methods.
- [x] **Task 13c** — Mac sidebar wired through `NavigationSplitView` with selection bound to `ChatSummary?` (not `.ID`) so the detail column has the full row data. Right-click context menu surfaces Mute + Leave; trackpad pull-to-refresh hits `ChatService.refresh()`. Mac `AppDependencies` got the same `timelineService(for:roomID:)` factory (per-room cache) and the same env keys (`\.appDependencies`, `\.currentSession`) as iOS, declared per-platform since the Mac `AppDependencies` class is a separate type. `MatronMacApp` injects both env values around `MacChatListView`. `MacChatRow` keeps its hover-tint state local. 2 new tests in `MatronMacTests/MacChatListViewTests.swift` cover the shared-VM contract and the `Hashable` selection state. Plan deviations: (a) `MacChatView` itself lands in Task 14c — for now the detail column is a `MacChatDetailPlaceholder` that primes the per-room `TimelineService` + `MediaService` caches via `.task(id: summary.id)` so the cache priming and selection contract are exercised before the chat view itself; (b) `+` toolbar button shows `MacNewChatPlaceholder` until Task 14 lands the real sheet.
- [x] **Task 14** — `NewChatSheet` (iOS) + `MacNewChatSheet` (Mac) shipped, both wired into their respective chat-list views' `+` toolbar sheets. Each derives the bot list from the first `ChatService.chatSummaries()` snapshot via `Set(snapshot.map(\.bot))` (Phase 2 has no separate bot directory) and calls `createChat(with:)` on tap. `onCreated` callback dismisses the sheet so the parent view can navigate to the new room. Plan deviations: (a) per-platform sheet classes — Mac and iOS each carry their own `AppDependencies` type, so the sheet body is duplicated rather than shared (only differences are `NavigationStack` on iOS vs sized `.frame` on Mac); (b) renamed plan's `@State private var error: String?` to `errorMessage` to avoid shadowing the catch-binding in `create(with:)`. 1 binding test per platform exercises construction + `onCreated` plumbing.
- [x] **Task 14c** — `MacChatView` shipped at `MatronMac/Features/Chat/MacChatView.swift`, replacing `MacChatDetailPlaceholder` in the sidebar's detail column. Hosts `ScrollView` + `LazyVStack` of `MacTimelineItemView` rows (right-click `contextMenu` → Copy + ShareLink) above `MacComposerView` with `.onDrop(of: [.image, .fileURL], delegate: ComposerDropDelegate(composer:))`. ⌘K toggles `composerVM.palettePinnedOpen` via a hidden zero-opacity Button (SwiftUI's accepted pattern for an invisible keyboard shortcut). ⌘R refresh wired through `NotificationCenter.matronCommand(.refresh)` (Task 14e expands the bus). Plan deviations: (a) plan suggested option (a) — `#if os(macOS)` branches in iOS `ComposerView` — but `ComposerView` lives in the iOS app target which doesn't build for macOS, so we used option (b): copy the iOS primitives (`SlashCommandPalette`, `TimelineItemView`) into the Mac target as `MacSlashCommandPalette` + `MacTimelineItemView`, and built a `MacComposerView` that uses `NSOpenPanel` for attachments instead of iOS's `PhotosPicker`/`fileImporter`; (b) `DropInfo` is a struct with no public init, not a protocol — so the plan's `MockDropInfo: DropInfo` test stand-in won't compile. Restructured the test to call the static `ComposerDropDelegate.loadURL(from:)` helper directly, which is the only branch with real logic; (c) landed a minimal `MacChatToolbar` (just title + ⓘ) and a minimal `MatronCommand` enum (just `.refresh`) so `MacChatView` compiles in this commit — Task 14d expands the toolbar; Task 14e expands the command bus; (d) promoted `Pasteboard` to public in `MatronDesignSystem` so `MacChatView`'s right-click Copy can reach it. 4 new tests cover URL extraction (happy + empty), palette toggle, and view construction.
- [x] **Task 14d** — `MacChatToolbar` expanded to the full §5.9 layout: sidebar toggle (left, posts `.toggleSidebar` to the command bus), title + session-meta-placeholder strip (center; meta strip lights up in Phase 5), refresh button (right, ⌘R) + search-field placeholder (full search lands Phase 6) + ⓘ info button (calls `onShowBotProfile`). Added `.toggleSidebar` to `MatronCommand` enum. 2 new tests cover toolbar construction (with closure plumbing) and the `.toggleSidebar` notification name. Plan deviation: refresh button still calls `viewModel.refresh()` directly — the menu-bar shortcut (Task 14e) will also fire `.refresh` over the bus, so trackpad-only Macs without the hardware shortcut don't depend on the listener being attached.
- [x] **Task 14e** — `MatronCommand` enum expanded to the full Phase-2 set (newChat, signOut, findInChat, slashCommand, toggleSidebar, increaseFontSize, decreaseFontSize, resetFontSize, verifyDevice, showRecoveryKey, refresh) with `.matronCommand(_:)` `Notification.Name` factory. `ChatCommands: Commands` struct mounts on `MatronMacApp`'s main scene via `.commands { ChatCommands() }` — each menu item posts to the `NotificationCenter` command bus. Help-menu items (Verify Device, Show Recovery Key) ship now so the menu shape is testable; their listeners land in Phase 3. 4 new tests cover distinct + namespaced notification names, the Phase-2 case set, round-trip post → observe, and `ChatCommands` construction. Plan deviation: `Notification.Name` extension stays `public` (the plan didn't specify visibility) — needed because `MacChatView`'s `.onReceive(...matronCommand(.refresh))` reaches it from the Mac app target and a future shared listener (e.g., font-size in design-system) would need it too.
- [x] **Task 15** — `BotProfileViewModel` shipped in `MatronShared/Sources/ViewModels/` (cross-platform; consumed by both iOS and Mac, the latter in Task 15b). Pure derivation over a `[ChatSummary]` snapshot — filters by `bot.matrixID` then sorts via the same recency-descending predicate `ChatListViewModel` uses (hydrated rooms first, nil-activity rooms by title). 4 unit tests cover happy filter + sort, nil-activity tiebreaker, hydrated-vs-nil ordering, and empty result. `BotProfileView` (iOS) at `Matron/Features/BotProfile/BotProfileView.swift` shows avatar placeholder + display name + Matrix ID (with `Pasteboard.copy(_:)` button) + "Start new chat" CTA + an "All chats" section listing each chat's title and relative-time. Wired from `ChatListView` as a `.sheet(item: $botProfileSummary)` — the ⓘ button on the pushed `ChatView` sets the state, the sheet snapshots the current `viewModel.groups` flat-list at presentation time, and the row tap / dismiss / "Start new chat" callbacks clear it. Plan deviations: (a) plan's test imported `Matron` but the SPM test target is `MatronViewModels` — corrected the import; (b) plan's test used a non-optional `Date` for `lastActivity` while `ChatSummary.lastActivity` is `Date?` — passed `Date()` (auto-promotes) and added two extra tests to nail down the `nil`-activity ordering; (c) plan's view body wraps the `List` directly with a `.navigationTitle` but no `NavigationStack` — wrapped in `NavigationStack` so the title actually renders when presented as a sheet; (d) added a "Copy Matrix ID" button using the now-public `Pasteboard` (per the handoff hint); (e) "Start new chat" from inside the sheet flips the `showingNewChat` state on `ChatListView` so the same `+`-button sheet is reused (no duplicate bot picker).
- [x] **Task 15b** — `MacBotProfileSheet` shipped at `MatronMac/Features/BotProfile/MacBotProfileSheet.swift`. Renders as a **full-window sheet** per spec §5.9 ("single main window — no third column"): VStack with a Mac-sized header card (80pt avatar, `.title2` display name, `.callout` Matrix ID + copy button, "Start new chat" CTA) above a `Divider` + `List` of "All chats". `.frame(minWidth: 480, idealWidth: 560, minHeight: 540, idealHeight: 640)` sizes it appropriately for a Mac. "Done" button in `.confirmationAction` toolbar slot with `.defaultAction` keyboard shortcut. Reuses the shared `BotProfileViewModel` from Task 15 unchanged. Wired into `MacChatListView` as `.sheet(item: $botProfileSummary)`; the ⓘ button on the detail-column `MacChatToolbar` sets the state. Row tap moves `selectedSummary` to the picked chat (Mac uses sidebar-driven selection, not pushed navigation), then dismisses; "Start new chat" flips `showingNewChat` to reuse the same `+`-button sheet. 2 new tests (`MatronMacTests/MacBotProfileSheetTests.swift`) cover construction with the shared view-model and round-trip invocation of all three callbacks. Plan deviations: (a) added a hidden cancellation-action button with `.cancelAction` keyboard shortcut so Esc closes the sheet (the plan only wired `.confirmationAction`'s Done button); (b) added a 4th constructor argument `onDismiss` (not in plan's earlier wiring snippet) so the row callback / new-chat callback / Done button can route through distinct intent without all colliding on `showingBotProfile = false`; (c) used `.sheet(item:)` keyed on `ChatSummary` (already `Identifiable` from Task 13) rather than a `Bool` toggle — keeps the binding atomic with the bot identity so re-opening for a *different* chat picks up the right bot.

## Bugbot pass — fixed 8 issues across composer / timeline / chat-vm

Cleared all 8 bugbot findings from the Phase 2 batches 3–5 review before Task 14 piles on top. One commit on `phase-2-chat-experience`. Per-bug summary:

1. **Composer palette reopens after command selection** (`MatronShared/Sources/ViewModels/ComposerViewModel.swift`) — `showPalette` now drops only leading whitespace instead of trimming both sides, so the trailing space `selectCommand(_:)` adds (`"/start "`) keeps the palette closed. New tests: `test_palette_staysClosed_afterCommandSelection`, `test_palette_isHiddenForCommandWithTrailingSpace`.
2. **Photo picker MIME wrong** (`Matron/Features/Chat/Composer/AttachmentPicker.swift` + `ComposerView.swift`) — `PhotosPicker` now initialises with `photoLibrary: .shared()`, and the selection handler picks the file extension from the item's `supportedContentTypes` (HEIC stays HEIC, PNG stays PNG) instead of falling back to `"photo.jpg"` and forcing `image/jpeg`.
3. **Send button enabled on whitespace-only** (`Matron/Features/Chat/Composer/ComposerView.swift`) — added `isSendable` (mirrors `send()`'s `.whitespacesAndNewlines` trim) and used it for both `.disabled(...)` and the accent-colour styling. New tests: `test_isSendable_matchesSendBehaviour_forWhitespaceOnly`, `test_send_doesNothing_forWhitespaceOnlyInput_andSendErrorStaysNil`.
4. **fileImporter URLs need security-scoped access** (`Matron/Features/Chat/Composer/ComposerView.swift` + `MatronShared/Sources/ViewModels/ComposerViewModel.swift`) — added `stageAndAttach(_:)` that wraps each URL with `start/stopAccessingSecurityScopedResource()` and stages bytes into a temporary file before calling `attachFiles(_:)`. Removed the silent `try?` in `attachFiles(_:)`; failures now surface via `sendError`. Added `ComposerViewModel.reportAttachmentError(_:)` so view-layer staging errors can flow through the same field while keeping `sendError` `private(set)`. New test: `test_reportAttachmentError_recordsSendError`.
5. **Display name has `@` prefix** (`Matron/Features/Chat/Rendering/TimelineItemView.swift`) — `displayName(for:)` now drops the leading sigil so `"@bot:server.com"` → `"bot"`. Promoted to `static internal` and tested directly. New file: `MatronTests/TimelineItemViewTests.swift` (4 tests covering happy path, missing sigil, no-colon malformed input, `@`-only edge case).
6. **Listener handle race / leak** (`MatronShared/Sources/Chat/TimelineServiceLive.swift`) — replaced double-assignment of `continuation.onTermination` with `TimelineLifecycleHolder`, which atomically owns both the setup `Task` and the SDK listener `TaskHandle` and cancels both on termination. Late-arriving registrations self-cancel if termination already fired. New file: `MatronShared/Tests/ChatTests/TimelineLifecycleHolderTests.swift` (4 tests).
7. **`myID` dead** (`MatronShared/Sources/Chat/TimelineServiceLive.swift`) — dropped the unused `myID` stored property and init parameter from `TimelineSnapshotListener` (the SDK already supplies `EventTimelineItem.isOwn`). Removed the `client.userId()` lookup at the call site.
8. **Infinite re-fetch on decode failure** (`MatronShared/Sources/ViewModels/ChatViewModel.swift`) — added `failedRequests: Set<URL>` so URLs whose `MediaService.swiftUIImage(for:)` returned nil are remembered and `image(for:)` short-circuits to `nil` on subsequent calls. New test: `test_imageRequest_doesNotLoop_whenMediaServiceReturnsNil`.

**Open follow-ups for the next agent (Tasks 14, 14c, 14d, 14e):**

- The bugbot spec for #8 hinted at a deeper refactor (e.g. making the image cache an actor). The current fix resolves the user-visible loop with the minimum diff; if Phase 3 starts surfacing decode-failed UI affordances, revisit whether `failedRequests` should evict on retry.
- `ComposerViewModel.reportAttachmentError(_:)` is iOS-call-site-specific today. When `MacChatView` (Task 14c) wires drag-and-drop, consider whether Mac drag failures should flow through the same surface or get their own field.
- The fix for #2 picks the first `supportedContentTypes` entry with a preferred extension. If Apple ever returns a parent abstract type before concrete ones, we'd want to skip it — keep an eye on this when iOS 19 lands.

## Bugbot pass — fixed 6 issues across composer / appdeps / timeline

Cleared the round-2 bugbot findings on top of the previous 8-issue pass. One commit on `phase-2-chat-experience` before batch 7 lands. Per-bug summary:

1. **`attachFiles` lacked security-scope wrap** (`MatronShared/Sources/ViewModels/ComposerViewModel.swift`) — moved `start/stopAccessingSecurityScopedResource()` *inside* `attachFiles(_:)` so every caller (Mac drop delegate, future call sites) gets the right scope handling. Kept the View's `stageAndAttach` wrap as defence-in-depth (it operates on the original URL before staging to a temp file). New test: `test_attachFiles_readsSecurityScopedURL_byWrappingItself`.
2. **`MediaService` not cached** (`Matron/App/AppDependencies.swift` + `MatronMac/App/AppDependencies.swift`) — added `mediaCache: [String: MediaService]` keyed by `session.userID`, mirroring `syncCache` / `timelineCache`. Each `MediaServiceLive` owns a 64 MB `NSCache`; a fresh instance per call defeated it. New files: `MatronTests/AppDependenciesTests.swift` + `MatronMacTests/MacAppDependenciesTests.swift` (2 tests each — identity + per-user isolation).
3. **Palette filter ignores leading whitespace** (`MatronShared/Sources/ViewModels/ComposerViewModel.swift`) — `filteredCommands` now strips leading whitespace before passing to `BotCommandCatalog.filter`, agreeing with `showPalette` (which already ignored leading spaces). Typing `"  /sta"` now returns matches instead of an empty palette. New test: `test_filteredCommands_stripsLeadingWhitespace`.
4. **Photo temp filename collision** (`Matron/Features/Chat/Composer/ComposerView.swift`) — extracted `static internal photoTempURL(ext:)` that embeds `UUID().uuidString` in the filename. Two photo selections of the same type now land at distinct temp URLs instead of the second clobbering the first before `attachFiles` reads it. New test: `test_photoTempURL_producesDistinctURLs_forSameExtension`.
5. **`replace(at:with:)` could duplicate IDs** (`MatronShared/Sources/Chat/TimelineServiceLive.swift` + `MatronShared/Tests/ChatTests/TimelineDiffApplicationTests.swift`) — when the SDK sets index `i` with an item whose id already lives at index `j != i`, we now remove the foreign occurrence first (adjusting the target if `j < i`) so `compactMap { byID[$0] }` can't yield the same `TimelineItem` twice. Mirrored in `SnapshotApplier`. New test: `test_set_withIDAlreadyElsewhere_doesNotDuplicate`.
6. **`sendImage` / `sendFile` missing `await`** (`MatronShared/Sources/Chat/TimelineServiceLive.swift`) — verified against the v26 SDK: `Timeline.sendImage(...)` and `sendFile(...)` are declared `throws -> SendAttachmentJoinHandle`, **not** `async throws` (cf. `matrix_sdk_ffi.swift` lines ~16041 / ~16051). The handle represents the in-flight upload that the SDK runs on its own runtime; the call returns synchronously. No code change needed — added inline comments documenting this so future readers don't try to "fix" the missing `await`.

**Open follow-ups for the next agent (Tasks 15, 15b — BotProfileView iOS + MacBotProfileSheet):**

- The defence-in-depth wrap in #1 means `start/stop` may run twice on a single URL when a View-layer wrapper is in play. Documented `URL.startAccessingSecurityScopedResource()`'s start/stop contract guards repeats, but if a future call site passes a URL where double-start has side effects, revisit.
- `MediaService` cache in #2 is per-`userID`. If Phase 3 introduces session refresh that issues a new `accessToken` for the same `userID`, the cached `MediaServiceLive` still holds the old `UserSession`. The cached instance reads `session` only inside `image(for:)` via `provider.client(for:session)`, so this is safe today (the provider keys off `userID` too) — but worth watching when Phase 3 swaps tokens.
- Bug #6 was a false positive after grepping the SDK source. If a future SDK upgrade actually moves `sendImage` / `sendFile` to `async throws`, the inline comments will be stale — re-check when bumping `matrix-rust-components-swift`.
