# Phase 3 — E2EE & Verification UX: Progress

This file tracks Phase 3 implementation progress.

**Plan:** `docs/superpowers/plans/2026-05-02-matron-ios-phase-3-e2ee-verification.md`

**Branch:** `phase-3-e2ee-verification` (stacked on `phase-2-chat-experience`)

## Status

Started 2026-05-03. Stacked on Phase 2 to overlap iteration with Phase 2 review.

## Phase 1 + 2 lessons that apply

- `AuthServiceLive(sessionStore:, basePath:)` constructor (not `keychain:`).
- `StoragePaths.groupContainer` etc. are `URL?` on iOS; macOS `appSupport` is non-optional.
- `ChatService.chatSummaries()` and `TimelineService.items()` return `AsyncThrowingStream`.
- `AppDependencies` splits container into `sdk-store/` (SDK) + `sessions/` (FileSessionStore).
- `slidingSyncVersionBuilder(versionBuilder: .native)` is REQUIRED on every `ClientBuilder()`.
- v26 SDK Room API: `displayName()` / `heroes()` / `latestEvent()` / `roomInfo()` (no `name()` / `activeMembersIds()` etc.).
- Swift 6 strict concurrency: no `@MainActor` `deinit` accessing isolated properties — expose `cancel()` / `stop()` and call from `View.onDisappear`.
- `MatronTests` and `MatronMacTests` are **not** standalone xcodebuild schemes — they're testables of `Matron` / `MatronMac` schemes. Use `xcodebuild test -scheme Matron -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest'`.
- iOS Simulator: `iPhone 17` (not 15 — Xcode 26 dropped iPhone 15).
- `MatronShared` exposes 7 library products (MatronAuth, MatronChat, MatronModels, MatronStorage, MatronSync, MatronViewModels, MatronDesignSystem). XcodeGen needs explicit per-product `package: MatronShared, product: <name>` declarations. If Phase 3 adds `MatronVerification`, add to Package.swift libraries AND to project.yml dependencies on both Matron + MatronMac targets.
- `Pasteboard.copy(_:)` is public in `MatronDesignSystem` — useful for the recovery-key copy button.
- `MatronStorage.LRUCache` is generic + public if Phase 3 needs bounded caches.
- `MatronCommand` enum (Mac) + `Notification.Name.matronCommand(_:)` exist for menu commands. `Verify Device…` and `Show Recovery Key…` slots may already be present in `Commands.swift`.
- iOS Simulator can't use Keychain without a signing team (Phase 1 deferred this; FileSessionStore is the workaround). If Phase 3 wires keychain-access-groups, expect the iOS simulator to hit `errSecMissingEntitlement -34018`.

## Tasks

See plan for canonical list. Updates land here per push.

- [x] **Task 1 — Verification DTOs** (2026-05-03). `DeviceInfo`, `DeviceTrustLevel`, `SasFlowState`, `SasEmoji`, `VerificationRequestSummary` in `MatronShared/Sources/Verification/VerificationDTOs.swift`. 5 unit tests in `Tests/VerificationTests/VerificationDTOsTests.swift`. **Plan deviation:** added the `MatronVerification` SPM library product + target in this commit (plan defers to Task 2 step 3) so the DTO file is reachable from a target and the commit can build/test in isolation. Wired the new product into both apps and both test targets in `project.yml`. SPM total: 150 tests (was 145). iOS scheme: 27. Mac scheme: 25. All green.
- [x] **Task 2 — `VerificationService` protocol + `FakeVerificationService`** (2026-05-03). Protocol in `MatronShared/Sources/Verification/VerificationService.swift`. Test fake `FakeVerificationService` (actor) + 6 protocol-shape tests in `Tests/VerificationTests/`. **Plan deviation:** test imports `MatronVerification` (not `MatronAuth` as the plan wrote) since Task 1 created the `MatronVerification` SPM target. Also tightened the fake's `actor` design: confirm/cancel mutate isolated state; `nonisolated` stream constructors hop back via `Task { await self.recordStart(...) }`. The plan's `var didCallStart` directly on the actor was fine, but I added `private(set)` so callers can observe state without mutating it. SPM total: 156 (was 150). iOS scheme: 27. Mac scheme: 25.
- [x] **Task 3 — `RecoveryKeyManager` + iCloud-syncable `KeychainStore`** (2026-05-03). Manager in `MatronShared/Sources/Verification/RecoveryKeyManager.swift`; `KeychainStore` extended with `synchronizable: Bool = false` (default preserves existing behaviour). 4 new tests (1 KeychainStore + 3 RecoveryKeyManager). **Plan deviations:**
  - SDK signature: plan calls `enableRecovery(waitForBackupsToUpload:progressListener:)` with `progressListener: nil`. Actual v26.04.01 signature is `enableRecovery(waitForBackupsToUpload:passphrase:progressListener:)` where `progressListener` is **non-optional** (`EnableRecoveryProgressListener`). Fixed by passing `passphrase: nil` and a private `NoopEnableRecoveryProgressListener`. UI-side progress reporting is deferred to the recovery-key view in a later task.
  - Test scope: plan only ships a `KeychainStoreTests` extension. I added `RecoveryKeyManagerTests` covering `currentKey()` round-trip + `storageKey` stability — the SDK-bound paths (`generateAndPersist`, `restore`) require a live `Client` so they're integration-tested in Phase 7. Manager exposes `storageKey` as `public static let` so tests (and a future migration) can reference it without a magic string.
  - Synchronizable test gating: the synchronizable round-trip half needs an iCloud Keychain entitlement that the SPM `swift test` host (and iOS Simulator) don't have. Probed: `SecItemAdd` returns `errSecMissingEntitlement (-34018)`. Test catches that specific status and `throw XCTSkip(...)`. Real-device coverage lands in Phase 7.
  - SPM total: 160 (was 156, +1 skipped). iOS scheme: 27. Mac scheme: 25.
- [x] **Task 4 — `VerificationServiceLive` + `SessionVerificationControlling`** (2026-05-03). Live impl in `MatronShared/Sources/Verification/VerificationServiceLive.swift`; protocol seam in `SessionVerificationControlling.swift`; `FakeSessionVerificationController` + 6 unit tests in `Tests/VerificationTests/`. **Plan deviations:**
  - Concurrency: plan stores `activeFlows` + `activeContinuations` behind an `NSLock` on the class. Swift 6 strict-concurrency rejects `NSLock.lock()` from async contexts. Switched to a private `actor FlowStore` inside `VerificationServiceLive`; class stays `@unchecked Sendable` because all mutation hops through the actor. Same trick on `FakeSessionVerificationController` — internal `actor Recorder` instead of NSLock-guarded bools.
  - Test-only init: tests call `VerificationServiceLive()` with no args. Added an internal no-arg init that leaves `provider`/`session` as `nil`; SDK-bound surfaces (`isThisDeviceVerified`, `incomingRequests`, `startSAS`) throw `.notConfigured` / yield empty under that init. Production callers use the public `init(provider:session:)`.
  - SDK signatures: plan calls `client.encryption().isVerified()` (doesn't exist on `Encryption`; `isVerified()` is on `UserIdentity`). Real v26 path: `client.encryption().verificationState() == .verified`. `requestSelfVerification()` also doesn't exist — replaced with `client.getSessionVerificationController()` then `requestDeviceVerification()` / `requestUserVerification(userId:)`. The plan's `IncomingVerificationListener: VerificationStateListener` is also wrong (that listener is for global state, not incoming requests) — actual incoming requests come via `SessionVerificationControllerDelegate.didReceiveVerificationRequest`. Wiring of the delegate is deferred to Task 4b's observer; `incomingRequests()` finishes empty for now.
  - SPM total: 166 (was 160). iOS scheme: 27. Mac scheme: 25.
- [x] **Task 4b — `DeviceVerificationRequestObserver`** (2026-05-03). Observer + `IncomingVerificationListening` protocol in `MatronShared/Sources/Verification/DeviceVerificationRequestObserver.swift`; production `LiveSessionVerificationDelegate` adapter alongside; `FakeIncomingVerificationListener` + 3 tests in `Tests/VerificationTests/DeviceVerificationRequestObserverTests.swift`. **Plan deviations:**
  - SDK delegate shape: plan extracts `(requestID, otherUserID, otherDeviceID, sdkController)` from a custom `onIncomingVerificationRequest` callback. v26.04.01's actual surface is `SessionVerificationControllerDelegate.didReceiveVerificationRequest(details: SessionVerificationRequestDetails)` — `details` carries `flowId`, `senderProfile.userId`, `deviceId` (non-optional), `deviceDisplayName`. The SDK doesn't hand back a per-request controller; the same `SessionVerificationController` is reused, so `LiveSessionVerificationDelegate` wraps the shared SDK controller in `LiveSessionVerificationController` and forwards. The remaining delegate callbacks (`didStartSasVerification`, `didReceiveVerificationData`, `didFinish`, `didCancel`, `didFail`) are no-ops in this commit — wiring them into the open SAS continuation will land with the SAS-state listener task; this commit stays scoped to the request-arrival adapter.
  - Test fake recording: plan's recorder pattern (an unguarded array on a fake) trips Swift 6 strict-concurrency. Switched `FakeIncomingVerificationListener` to an `NSLock`-guarded buffer accessed synchronously from `onUpdate` — keeps tests synchronous (no actor flush dance) since the protocol's `onUpdate` is itself synchronous.
  - SPM total: 169 (was 166). iOS scheme: 27. Mac scheme: 25.
- [x] **Task 5 — `RecoveryKeyViewModel` + iOS `RecoveryKeyView`** (2026-05-03). Cross-platform `@Observable @MainActor` VM in `MatronShared/Sources/ViewModels/RecoveryKeyViewModel.swift`; iOS view in `Matron/Features/Verification/RecoveryKeyView.swift`. 9 SPM unit tests + 2 iOS binding tests. **Plan deviations:**
  - `keysMatch` is `nonisolated public static` — pure bytewise XOR-then-OR with no actor state. Letting it be `@MainActor`-isolated forced the unit tests into a `@MainActor` ceremony for a function that could just as well be a free function. `nonisolated` keeps the call sites clean while the rest of the type stays main-actor-only.
  - Added two extra constant-time-compare tests (unequal-length both directions; first-byte mismatch) to lock the bytewise loop's correctness — the plan's tests only exercise the equal-length match/mismatch path.
  - View uses `Color(.systemGray6)` directly instead of `Color.matronCodeBg` — the design-system alias is `internal` to `MatronDesignSystem`, and promoting it to `public` for one consumer felt over-broad. The Mac view will mirror with `Color(NSColor.controlBackgroundColor)` (same pattern the design-system uses internally).
  - `phase` retains a `.done` case from the plan's enum but the iOS view doesn't currently render anything different for `.done` (dismiss happens via the explicit "Confirm" / "Done" buttons). Kept the case so the Mac view's success-animation path (`generatePhase == .confirmed` after auto-advance) has clear analogue surface to use.
  - SPM total: 178 (was 169, +9). iOS scheme: 29 (was 27, +2). Mac scheme: 25.
- [x] **Task 5c — `MacRecoveryKeyView` + `PasteDetector`** (2026-05-03). Mac sheet in `MatronMac/Features/Verification/MacRecoveryKeyView.swift`; `MatronPasteboardReading` protocol + `LiveNSPasteboard` + `PasteDetector` in `NSPasteboardWrapper.swift`; 6 tests in `MatronMacTests/MacRecoveryKeyViewTests.swift`. Same shared `RecoveryKeyViewModel` from Task 5; only the SwiftUI layer differs. **Plan deviations:**
  - Plan's protocol name `NSPasteboardReading` collides with AppKit's same-named protocol on `NSPasteboard`. Renamed to `MatronPasteboardReading` so callers don't have to disambiguate. The fake-pasteboard test double conforms to the renamed protocol.
  - Plan test asserts `XCTAssertTrue(vm.canFinish)` after `PasteDetector` auto-advances `generatePhase` to `.confirmed`. That's contradictory: `canFinish` requires `generatePhase == .reenter`, so once we leave `.reenter` the predicate is intentionally `false`. Replaced with `XCTAssertFalse(vm.canFinish)` and a comment explaining the gate. The meaningful assertions (`reenteredKey` stored, phase advanced) still hold.
  - Added two extra tests: paste no-ops outside `.generate / .reenter` (defensive guard against stray Paste-button presses in another mode); paste no-ops on empty / nil clipboard. Plus two SwiftUI body-composition smoke tests (one per mode) mirroring `MacBotProfileSheetTests`.
  - SPM total: 178. iOS scheme: 29. Mac scheme: 31 (was 25, +6).
- [x] **Task 6 — `SasViewModel` + iOS `SasView`** (2026-05-03). Cross-platform `@Observable @MainActor` VM in `MatronShared/Sources/ViewModels/SasViewModel.swift`; iOS view in `Matron/Features/Verification/SasView.swift`. 5 SPM unit tests + 3 iOS binding tests. **Plan deviations:**
  - Widened `MatronViewModels` to depend on `MatronVerification` (Package.swift + ViewModelTests test target). Plan flagged the choice between widening the dep and using closures à la `RecoveryKeyViewModel`; widening is the right call here because `SasViewModel.state` is typed `SasFlowState` — the public surface depends on the verification module no matter how confirm/cancel route.
  - Added two extra tests beyond the plan: `test_cancel_invokesCallback_withReason` pins the reason-string plumbing; `test_emojiOrder_isPreservedFromStream` locks in stream-order for emoji rendering (bugbot-bait around ID-based reordering).
  - View uses `Color(.systemGray6)` directly (Task 5 precedent — `Color.matronCodeBg` is `internal` to `MatronDesignSystem`).
  - No `deinit`: `View.task(id:)` auto-cancels the observe task on disappear, and the producing service owns the stream's continuation. No need for an explicit `cancel()` like `ChatListViewModel` since there's no manual `Task` storage.
  - SPM total: 183 (was 178, +5). iOS scheme: 32 (was 29, +3). Mac scheme: 31.
- [x] **Task 7c — `MacSasView`** (2026-05-03). Mac sheet in `MatronMac/Features/Verification/MacSasView.swift`; 4 binding tests in `MatronMacTests/MacSasViewTests.swift`. Same shared `SasViewModel` from Task 6; only the SwiftUI layer differs. **Plan deviations:**
  - Plan calls for "no new logic-test in this step" (state machine already covered by `SasViewModelTests`). Added 4 SwiftUI body-composition smoke tests anyway, mirroring the `MacRecoveryKeyViewTests` and `MacBotProfileSheetTests` precedent — body composition is the kind of regression that Mac-side surface bugs (sheet sizing, NSColor on iOS-only types, etc.) tend to manifest as.
  - Background uses `Color(NSColor.controlBackgroundColor)` (mirrors design-system internal alias on macOS, same pattern as `MacRecoveryKeyView`).
  - File guarded with `#if os(macOS)` so the test bundle's iOS-leaked sources don't trip on `NSColor`. Same guard pattern as `MacRecoveryKeyViewTests` / `MacChatListViewTests`.
  - SPM total: 183. iOS scheme: 32. Mac scheme: 35 (was 31, +4).
- [x] **Task 7 — `PostLoginVerificationView` + Mac analogue + onboarding gate** (2026-05-03). iOS view in `Matron/Features/Onboarding/PostLoginVerificationView.swift`; Mac analogue in `MatronMac/Features/Onboarding/MacPostLoginVerificationView.swift`. Both apps wire a `verifyDone` `@State` flag in `MatronApp` / `MatronMacApp` that gates the chat list behind verification; the flag is persisted per-user in `UserDefaults` (`matron.verify-done.<userID>`) so the gate only fires once per (app, user). 3 iOS + 3 Mac binding tests. **Plan deviations:**
  - Mirrored the entire flow on Mac (plan only describes iOS). Same shape, with `MacRecoveryKeyView` / `MacSasView` swapped in and a `frame(width:height:)` + `keyboardShortcut(.return)` on the primary action for native Mac feel. Without the Mac mirror, signing in on the Mac would land users straight in the chat list with no chance to set up E2EE.
  - Self-verification request ID: plan suggests `"self-verify:\(userID):\(deviceID)"`. Switched to plain `session.userID` because that's the cache key `VerificationServiceLive.startSAS` registers under for self-verification flows — using the namespaced ID would cause confirm/cancel to look up an entry that doesn't exist. Locked the choice with the iOS / Mac body-composition tests; the live-stack integration tests in Phase 7 will catch any future drift.
  - `verifyDoneKey(for:)` is `static` on the view (not buried in the `MatronApp`) so the test suite can lock the key shape — the gate's silent-no-op failure mode is bad enough to deserve a compile-coupled regression check.
  - Sign-out (both apps) clears the persisted flag for the active session so a deliberate sign-out + back-in re-runs the gate. Without this, a user who botched verification on a prior login would have no way to retry from the UI.
  - Bootstrap restores the flag for the previously-signed-in session so a relaunch with an existing session lands directly in the chat list rather than re-prompting verification.
  - `MatronStorage` is now imported in both app targets (for the `KeychainStore` constructor inside the navigation destinations). Already a transitive dependency, so no `project.yml` change.
  - SPM total: 183. iOS scheme: 35 (was 32, +3 — `PostLoginVerificationViewTests` x3). Mac scheme: 38 (was 35, +3 — `MacPostLoginVerificationViewTests` x3).
- [x] **Task 8 — `VerificationCenter` orchestrator** (2026-05-03). `@Observable @MainActor` orchestrator in `MatronShared/Sources/ViewModels/VerificationCenter.swift`. Observes `VerificationService.incomingRequests()`, exposes `pending: [VerificationRequestSummary]` for the chat-list / sidebar banner, and dismisses cancel-then-remove (load-bearing — the partner device shows a stale waiting UI otherwise). 4 SPM unit tests in `Tests/ViewModelTests/VerificationCenterTests.swift`. **Plan deviations:**
  - Plan files the source under `Matron/Features/Verification/` (iOS-only target). Moved to `MatronShared/Sources/ViewModels/` because Task 9b explicitly requires the same `VerificationCenter` instance to drive the Mac sidebar banner — putting it under the iOS target would force a duplicate Mac-side type or a circular reach across app targets. Mirrors the cross-platform home of `RecoveryKeyViewModel` / `SasViewModel`.
  - Plan test path is `MatronTests/VerificationCenterTests.swift` (iOS host); moved to `MatronShared/Tests/ViewModelTests/` for the same shared-home reason. The host-app target test bundles already pull in `MatronViewModels`, so the orchestrator is reachable from either side without further wiring.
  - Local fake instead of importing `FakeVerificationService` from `VerificationTests` — test targets can't reach into each other's internal fakes. Duplicated minimally as `ScriptedVerificationService` (actor) covering the dismiss + observation flows.
  - `start()` is idempotent (re-firing cancels the prior task and starts fresh) — plan's snippet missed this, which would leak the prior observation task on a SwiftUI view remount. Added `test_start_dedupesByRequestID` to lock the cache-by-id guard inside the loop and `test_stop_isSafe_withoutStart` to guard the `nil`-task no-op.
  - SPM total: 187 (was 183, +4). iOS scheme: 35. Mac scheme: 38. (No host-app test churn for Task 8 — those tests will land in Task 9 / 9b when the banner ships.)
- [x] **Task 9 — `VerificationBanner` (iOS) + `ChatListView` wire-up** (2026-05-02). Banner in `Matron/Features/Verification/VerificationBanner.swift`; `ChatListView` now hosts an optional `verificationCenter: VerificationCenter` parameter, renders one banner per `pending` summary above the existing list content, and presents `SasView` via `.sheet(item: $sasSummary)`. `MatronApp` constructs the center from `VerificationServiceLive(provider: dependencies.clientProvider, session: session)` once the post-login gate has cleared. 4 binding tests in `MatronTests/VerificationBannerTests.swift`. **Plan deviations:**
  - Made `verificationCenter` optional on `ChatListView` so the existing `ChatListViewBindingTests` (which construct via `ChatListView(viewModel: vm)`) keep compiling. The plan's `@State var verificationCenter` would have required wiring a fake into every existing binding test. Mirrors the existing `onSignOut: (() -> Void)? = nil` opt-in pattern.
  - Extracted the existing list states into a `chatListContent` `@ViewBuilder` so the new top-of-view banner section sits above all four branches (loading / error / empty / populated) inside a single `VStack(spacing: 0)`. The plan's snippet only hints at "above the `List`," but the banner needs to be visible in the loading + empty states too — otherwise a user with a pending verification request would see nothing while sliding-sync warmed up.
  - `start()` / `stop()` wired to `.onAppear` / `.onDisappear` (plan-mandated) — Swift 6 strict concurrency forbids `@MainActor deinit` reaching isolated state, same lesson as `ChatListViewModel.cancel()`.
  - Banner-presented SAS sheet builds `VerificationServiceLive` inline + uses `acceptIncoming(requestID: summary.id)` (the live impl already wires `acceptVerificationRequest` + `startSasVerification` into the cached controller registered when the request arrived). `onFinished` clears `sasSummary` so `.verified` auto-dismisses (the bug `b0b15d1` fixed for the onboarding gate).
  - SPM total: 187. iOS scheme: 39 (was 35, +4 — `VerificationBannerTests`). Mac scheme: 38.
- [x] **Task 9b — `MacVerificationBanner` + `MacChatListView` wire-up** (2026-05-02). Banner in `MatronMac/Features/Verification/MacVerificationBanner.swift`; `MacChatListView` now hosts an optional `verificationCenter: VerificationCenter` parameter, renders one banner per `pending` summary above the sidebar inside the leading column of `NavigationSplitView`, and presents `MacSasView` via `.sheet(item: $sasSummary)`. `MatronMacApp` constructs the center from `VerificationServiceLive` once the post-login gate has cleared. 4 binding tests in `MatronMacTests/MacVerificationBannerTests.swift`. **Plan deviations:**
  - Skipped the snapshot-test step (plan §9b Step 3). `MatronMacTests` doesn't depend on `SnapshotTesting` (it's an SPM-only dep on the design-system suite), and adding the dep here for one test would broaden the bundle's surface for marginal benefit. The 4 body-composition + callback tests cover the same regressions as the iOS analogue and mirror the existing `MacRecoveryKeyViewTests` / `MacSasViewTests` precedent.
  - Made `verificationCenter` optional on `MacChatListView` so existing binding tests keep compiling (mirrors the iOS `verificationCenter` opt-in + the existing nil-safe environment pattern).
  - Wrapped the existing `sidebar` in a new `sidebarColumn` `@ViewBuilder` that stacks banners above when the center has pending entries; falls straight through to `sidebar` otherwise. This keeps the rest of the existing sidebar logic (selection, hover state, list style) untouched.
  - `start()` / `stop()` wired to `.onAppear` / `.onDisappear` (Swift 6 / `@MainActor deinit` constraint).
  - File guarded with `#if os(macOS)` so an iOS-leaked source path can't trip on `NSColor` (mirrors `MacRecoveryKeyView` / `MacSasView`).
  - Banner background uses `Color(NSColor.windowBackgroundColor).opacity(0.9)` per plan; sheet construction mirrors the iOS Task 9 `acceptIncoming` flow.
  - SPM total: 187. iOS scheme: 39. Mac scheme: 42 (was 38, +4 — `MacVerificationBannerTests`).
- [x] **Task 9c — Help menu wire-up (Mac)** (2026-05-02). Added two `.onReceive` listeners on `MatronMacApp`'s WindowGroup root for `.matronCommand(.verifyDevice)` and `.matronCommand(.showRecoveryKey)`, mirroring the existing `.signOut` wire-up. Each listener flips a `@State` sheet flag; the sheet body builds a fresh self-verification SAS flow (`MacSasView`) or recovery-key restore flow (`MacRecoveryKeyView`) against the active session. 2 round-trip tests added to `MacCommandsTests` (`test_post_verifyDevice_notifiesObserver` + `test_post_showRecoveryKey_notifiesObserver`). **Plan deviations:**
  - The plan's Step 1 + Step 2 (`MatronCommand` enum + `.commands` wiring) were already shipped in Phase 2 — `Commands.swift` already has the `.verifyDevice` + `.showRecoveryKey` cases and `ChatCommands` already posts them from the `CommandGroup(replacing: .help)` block. Phase 3 only needed Step 3: the listeners + sheets.
  - Plan suggests `RecoveryKeyViewModel(mode: .restore, generate: { "" }, restore: { _ in })` for Show Recovery Key. Wired the real `RecoveryKeyManager.restore(usingKey:)` instead so the menu item is functionally useful today (the user can paste their key and re-unlock backup) — Task 11's full re-auth-then-reveal "show me the locally-stored key" flow lives in DeviceSettingsView, but the restore path is a valid use of this menu item until then. Empty closure would have made the item a UI no-op.
  - Plan describes a "no other device reachable → fall back to recovery-key restore" branch on Verify This Device. Implementing the live device-list query needs the SDK's device-fetch surface, which is out of scope for the menu wire-up. Today the sheet always presents `MacSasView`; a stale request surfaces back through the existing `.cancelled(reason:)` UI. Recovery-key fallback for the "no other device" case lands with Task 11.
  - Both sheets short-circuit to a "sign in first" message when `session == nil` — listener lives on the WindowGroup root so it's attached regardless of which child view is on screen, but the sheet body wants a session to construct anything useful.
  - Imported `MatronStorage` for `KeychainStore` (already a transitive dep of the Mac target via `MatronViewModels`).
  - SPM total: 187. iOS scheme: 39. Mac scheme: 44 (was 42, +2 — `MacCommandsTests` round-trip pair).
- [x] **Task 10 — `ChatView` per-bot verification banner (iOS + Mac)** (2026-05-03). Extended `VerificationService` with `isUserVerified(matrixID:)` (live impl reads `client.encryption().userIdentity(userId:fallbackToServer:false)?.isVerified()`; fake defaults to `false` per §7.5 + has a test-seam `setUserVerified(_:for:)`). `ChatView` (iOS) + `MacChatView` both gained optional `verificationService:` + `botMatrixID:` params and a `.task` that calls `evaluateBotVerification()`; when the bot is unverified, an inline banner sits above the timeline (above the existing error banner) with a "Verify" button that presents `SasView` / `MacSasView` for that bot user. Hosts (`ChatListView` / `MacChatListView`) wire the params from `verificationCenter?.service` (re-using the FlowStore that any incoming request was registered against) with a fresh-impl fallback for previews. 2 SPM unit tests + 2 iOS binding tests + 2 Mac binding tests. **Plan deviations:**
  - Plan Step 1 says "use `isThisDeviceVerified()`" with a note that the implementer should check spec §7.1 first. Spec §7.3 is unambiguous: "Chat view shows an inline banner for unverified bot devices" — that's per-bot, not per-account. So the plan's per-account read would have been wrong (every chat would have shown the same global banner). Added `isUserVerified(matrixID:)` to the `VerificationService` protocol + live + fake + the `ScriptedVerificationService` in `VerificationCenterTests`.
  - Plan Step 2 just says "render a banner above the timeline" — fleshed out as `botVerificationBanner` (HStack with shield icon + "Verify" button + accessibility-combined element) and a sheet that presents the SAS flow inline (mirrors `PostLoginVerificationView.sasWithOtherDevice` + `MatronApp.verifyDeviceSheet`). On `onFinished` the sheet re-evaluates `isUserVerified` so a successful match auto-hides the banner without a remount.
  - Three-state `isBotVerified: Bool? = nil` (not `false`) so the banner doesn't briefly flash for already-verified bots during the async query. Throws → `false` so a flaky network defaults to "show the banner" — matches §7.5 "nothing auto-trusted".
  - `isUserVerified` uses `fallbackToServer: false` to avoid blocking the banner on a network round-trip; the local crypto store has the identity once any prior to-device or `/keys/query` has landed (sliding-sync warmup drives that).
  - Reused `VerificationCenter.service` from the host (round-4 bugbot fix made it `public let`). Building a fresh `VerificationServiceLive` for the SAS sheet would have hit an empty FlowStore — the same trap the chat-list incoming-request banner already documents.
  - SPM total: 189 (was 187, +2 — `VerificationServiceFakeTests` x2). iOS scheme: 41 (was 39, +2). Mac scheme: 46 (was 44, +2).
- [x] **Task 11 — `DeviceSettingsView` + Mac analogue + Settings entry** (2026-05-03). iOS view in `Matron/Features/Settings/DeviceSettingsView.swift`; Mac analogue in `MatronMac/Features/Settings/MacDeviceSettingsView.swift`. Three sections each (Account / Encryption / Recovery key); `LabeledContent` rows surface `userID`, `deviceID`, `homeserverURL.host`; an Encryption section drives `verificationService.isThisDeviceVerified()` (✓/⚠/spinner three-state) and a "Show recovery key" button that reveals the locally-stored key on tap. iOS host (`ChatListView`) gains a Settings entry in the existing overflow menu (above Sign Out per HIG); Mac host (`MatronMacApp`) swaps the Help → Show Recovery Key… sheet body from `MacRecoveryKeyView(.restore)` to `MacDeviceSettingsView`. 3 iOS + 3 Mac binding tests. **Plan deviations:**
  - Plan's snippet builds the view with `recoveryKeyManager: RecoveryKeyManager`. Switched to a closure parameter (`currentRecoveryKey: () throws -> String?`) so the view stays trivially testable without a real Keychain — mirrors the `RecoveryKeyViewModel` `generate` / `restore` closure pattern. Hosts forward `{ try mgr.currentKey() }`; the closure indirection costs nothing at the call site.
  - Plan's reveal collapses success / nil / error into `(try? recoveryKeyManager.currentKey()) ?? "(not stored)"`. That's the kind of recovery-key data-loss path bugbot watches for: a Keychain read failure would silently look like "no key stored", masking a real bug. Split into three explicit UI states — `revealedKey` / `revealError` / nil — so the user sees something different for each branch.
  - Plan files this under `Matron/Features/Settings/` (iOS only). Mirrored to `MatronMac/Features/Settings/MacDeviceSettingsView.swift` since the Help-menu shim from Task 9c needs a real "show recovery key" surface — and the spec §6 Settings → Devices section applies equally to both apps.
  - Mac sheet ships fixed-size 480×420 with an explicit Done button + `Color(NSColor.windowBackgroundColor)` divider footer (mirrors `MacRecoveryKeyView` precedent). Form uses `.formStyle(.grouped)` so AppKit gets the inset-grouped chrome it expects.
  - Three-state `isVerified: Bool? = nil` (not `false`) — same pattern from Task 10 so the row doesn't briefly flash "not verified" for actually-verified devices during the async query.
  - iOS host wraps `DeviceSettingsView` in a `NavigationStack` inside the sheet so the navigationTitle renders + the Done toolbar button plumbs cleanly. Settings entry is gated on `deps != nil, session != nil` so previews / tests with no environment stay clean.
  - Imported `MatronStorage` in `ChatListView` (was already a transitive dep via `MatronViewModels`; explicit import keeps the `KeychainStore(...)` call site readable).
  - SPM total: 189. iOS scheme: 44 (was 41, +3 — `DeviceSettingsViewTests`). Mac scheme: 49 (was 46, +3 — `MacDeviceSettingsViewTests`).
- [x] **Task 14 — manual-tests.md additions** (2026-05-03). Phase 3 section appended to `manual-tests.md` covering first-device generate, second-device restore, multi-device SAS, bot verification, trust posture, Mac chrome, iCloud Keychain auto-restore. **Plan deviations:**
  - Plan's "Show Recovery Key…" step pointed at `MacRecoveryKeyView` in restore mode. Updated to point at `MacDeviceSettingsView` (Task 11 swapped the menu sheet body).
  - Added two regression-guard sections beyond the plan: a multi-account section that covers bugbot finding #4 (per-user storage key) and a verification-gate section that covers bugbot finding #2 (sync runs during the gate).
  - Marked Phase 2's "Verification UX (Phase 3)" placeholder in the "NOT tested" list as struck through with a forward-pointer to the Phase 3 section. The "NOT tested in Phase 3" subsection at the end captures what we still don't cover (live device-list query, Phase 7 Settings reauth, snapshot pixel-mismatch on macos-15 CI).
  - SPM: 189. iOS: 44. Mac: 49.
- [x] **Task 13 — Mac Keychain entitlement + setup-time probe** (2026-05-03). Verified `MatronMac/App/MatronMac.entitlements` already declares `keychain-access-groups → $(AppIdentifierPrefix)chat.matron.mac` (no entitlement change needed — the entry was added when the file was first generated). Added `KeychainProbe.run(keychain:)` in `MatronShared/Sources/Storage/KeychainProbe.swift` — a setup-time helper that round-trips a UUID-tagged value through any `SessionStore` (set → get → compare → delete) and throws a typed `KeychainProbeError` on failure. Wired it into `MatronMacApp.bootstrap()` ahead of the session-restore step, with a 2s `withThrowingTaskGroup` timeout so a hypothetical Keychain unlock prompt can't strand the user on the indefinite ProgressView. Probe failures populate a new `bootstrapError: String?` `@State` that the body's first branch renders as `MacKeychainSetupErrorView` — a hard gate (not a dismissable banner) blocking the SignInView so the user can't generate a recovery key against a broken Keychain. 4 SPM unit tests in `Tests/StorageTests/KeychainProbeTests.swift` cover positive (real `KeychainStore`) + 3 negative paths (set fail / get fail / round-trip mismatch) via an in-memory `ThrowingSessionStore` test double. **Plan deviations:**
  - Plan Step 1 says "modify `MatronMac/MatronMac.entitlements`." File path was actually `MatronMac/App/MatronMac.entitlements` (matches the Mac target's `CODE_SIGN_ENTITLEMENTS` setting in `project.yml`). The entry was already present from the initial Mac target setup — Step 1 is a verification, not a change.
  - Plan Step 2 says "fatal-but-helpful error." Took that as "fatal-feeling UI" not `fatalError()` — the constraint section explicitly forbids the trap because the user needs to read the message. Implemented as `MacKeychainSetupErrorView` (red shield icon + setup-doc pointer + raw error message in monospaced caption font for copy-paste). Hard gate via the new top-priority `bootstrapError` branch on the body so it can't be dismissed past.
  - Probe lives in `MatronStorage` (not `MatronMacApp`) so the SPM test bundle can drive it without a Mac app dependency. Accepts `any SessionStore` (not `KeychainStore` directly) so the negative-path tests inject a `ThrowingSessionStore` double — same pattern Phase 3 used for the verification fakes.
  - Probe key (`matron.keychain-probe.v1`) is a constant — a half-completed prior run's leftover entry is overwritten on the next launch rather than accumulating UUID-named ghost entries. Probe always attempts cleanup on the way out (even when the round-trip mismatches or `get` fails) so `set` writes never leak.
  - 2s timeout via `withThrowingTaskGroup` + a private `KeychainProbeTimeout` sentinel — distinct from `KeychainProbeError.getFailed` so the catch arms render a timeout-specific message ("Keychain access timed out — see docs/setup-mac.md"). The `cancelAll()` after `next()` cleans up the loser task either way.
  - The synchronizable Keychain probe will hit `errSecMissingEntitlement (-34018)` on the SPM `swift test` host (no signing team to expand `$(AppIdentifierPrefix)`). The SPM tests therefore exercise the probe against a non-synchronizable `KeychainStore(service: "chat.matron.test.probe")` for the positive path — same precedent as `KeychainStoreTests.test_setAndGet`. The synchronizable runtime path is what the Mac app actually exercises and what the Task 14 manual-test (iCloud Keychain auto-restore) covers end-to-end.
  - `docs/setup-mac.md` referenced in the error UI doesn't exist yet — same forward reference the plan's Step 2 spec uses. Authoring it is a doc-only follow-up out of scope for the wiring task; today the message at least directs developers to the right doc-name once it lands.
  - SPM total: 193 (was 189, +4). iOS scheme: 44. Mac scheme: 49 (no Mac-side test added — the probe lives in `MatronShared` and is tested at the SPM layer per the task spec; entitlements are an integration concern explicitly out of scope for unit testing).
- [x] **Bugbot fix-up — round 4** (2026-05-03). Four findings on PR #2:
  1. Mac generate-flow Confirm only flipped local phase to `.confirmed` and relied on a 600ms `.task` auto-dismiss racing with `.onChange(of: reenteredKey)`. Mirrors iOS — Confirm now calls `onFinished()` directly.
  2. Sync service was only started on the post-verify branch in both iOS + Mac hosts. SAS verification needs sliding-sync running to exchange to-device events, so verify-with-other-device hung when `verifyDone == false`. Started sync on the verification-gate branch on both platforms.
  3. (Already fixed in round-3) Mac restore Done was disabled — confirmed working.
  4. `RecoveryKeyManager.storageKey` was a single shared `"matron.recovery-key"` constant. With `kSecAttrSynchronizable=true` a second account on the same device would overwrite the first user's recovery key and propagate the loss across all linked devices. Replaced with `storageKey(for: userID)` so each user's key lives in its own scoped Keychain entry. `RecoveryKeyManagerTests` updated to cover per-user scoping.
  - SPM: 189. iOS: 44. Mac: 49 (no test count changes — bugbot fixes are behavior-only or update existing tests).
- [x] **Task 12 — Snapshot tests, Mac verification chrome** (2026-05-03). 6 new snapshot tests (2 in `MatronMacTests/MacSasViewSnapshotTests.swift` for `MacSasView` `.readyForEmoji` + `.verified`; 4 in `MatronMacTests/MacRecoveryKeyViewSnapshotTests.swift` for `MacRecoveryKeyView` `.show` / `.reenter mismatch` / `.confirmed` / `.restore`). Mirror SPM-side `assertVariants` helper duplicated into `MatronMacTests/SnapshotHelpers.swift` (Mac branch only — the SPM helper lives in a different test target and isn't reachable from the Xcode test bundle). 18 baseline PNGs recorded under `MatronMacTests/__Snapshots__/` (3 variants per test: light × dark × accessibility5). **Plan deviations:**
  - Phase 2 Task 9b note ("skipped Mac snapshot tests — `MatronMacTests` doesn't link `SnapshotTesting`, marginal benefit") was the blocker the constraints flagged. Resolved here by adding `swift-snapshot-testing` to `project.yml` `packages` and as an explicit dep on the `MatronMacTests` target. The package was already resolved transitively via `MatronShared/Package.swift`, so xcodebuild's package graph picked up `1.19.2` without a new resolve.
  - `xcodebuild test` does **not** inherit the parent shell's env into the test runner (unlike `swift test`). The skip therefore only works via the documented `TEST_RUNNER_*` prefix: `TEST_RUNNER_MATRON_SKIP_SNAPSHOT_TESTS=1 xcodebuild test …`. CI's `mac-build-and-test` job updated to pass it that way; helper-file docblock notes the gotcha so future readers don't trip on it. The plan snippet's `MATRON_SKIP_SNAPSHOT_TESTS=1` invocation would silently still pixel-compare against baselines under `xcodebuild`.
  - Plan snippet drives the SAS VM into target state via `let exp = expectation(...); Task { @MainActor in await vm.observe(); exp.fulfill() }; wait(for: [exp], timeout: 1)`. Switched to plain `await vm.observe()` from an `async` test method — same pattern existing `MacSasViewTests` uses. The producer's `c.finish()` lets `observe()` return as soon as the state has been published; the expectation/Task dance is unnecessary ceremony.
  - Did NOT add a `MacVerificationBannerSnapshotTests` even though Task 9b's plan listed it (and Task 12 §Step 0 references it as "already created in Task 9b"). Phase 2's progress entry explicitly skipped that subtask; staying scope-minimal here means Task 12 ships only the two files Task 12 asks for. Adding the banner snapshot is a one-liner follow-up if desired (the dep wiring is now in place).
  - SPM-side `SnapshotVariants.swift` left untouched (the constraint says don't modify it unless required). Helper duplication is a deliberate trade against widening the SPM target's product surface or moving the helper into a shared library product just for one Xcode test bundle.
  - CI yml updated: `mac-build-and-test` job gets the `TEST_RUNNER_MATRON_SKIP_SNAPSHOT_TESTS: "1"` env, mirroring the `shared-package-tests` job's pattern. Without this, the new snapshots would pixel-fail on macos-15 vs macOS 26 renderer drift on every PR.
  - SPM: 193. iOS: 44. Mac: 55 (was 49, +6 — `MacSasViewSnapshotTests` x2 + `MacRecoveryKeyViewSnapshotTests` x4). 18 new PNG baselines in `MatronMacTests/__Snapshots__/`.
  - **New deps added:** `swift-snapshot-testing` (already transitive via `MatronShared/Package.swift`; project.yml change just promotes it to a top-level package so the Xcode test bundle can import it).
- [x] **B1 fix-up — wire SDK delegate so SAS verification is actually functional end-to-end** (2026-05-03). Expert-QA found that `LiveSessionVerificationDelegate` was constructed nowhere and never registered with the SDK: `incomingRequests()` finished empty (sidebar / chat-list banner dead), `startSAS` / `acceptIncoming` produced `.requested` then nothing forever (SAS sheet hung at "Starting verification…"), and `confirmEmojiMatch` synthesised `.verified` itself without an SDK round-trip (security + UX bug). All three findings were the same root cause — the delegate wiring deferred from Task 4b never landed. **What changed:**
  - `VerificationServiceLive` gained a `start()` method that fetches `client.getSessionVerificationController()`, retains it on the FlowStore, and registers a `LiveSessionVerificationDelegate` against it. `start()` is idempotent (FlowStore-mediated check on whether the SDK controller has been cached). Class now also conforms to a new internal `SessionVerificationFlowRouting` protocol — the delegate is decoupled from the concrete service so tests substitute a fake router.
  - `LiveSessionVerificationDelegate` rewritten to translate every SDK callback (`didReceiveVerificationRequest` / `didStartSasVerification` / `didReceiveVerificationData` / `didFinish` / `didCancel` / `didFail`) into the matching `routeSas…` entry point on the router via `Task { await … }` (SDK callbacks come in on the SDK's own thread; FlowStore is an actor). `router` reference is `weak` so the SDK's callback-handle retention can't pin the service after `signOut()` clears the cache.
  - `FlowStore` extended with `activeFlowID: String?` (lifecycle callbacks don't carry a flow ID — the SDK has one shared SessionVerificationController and only one flow runs on it at a time), `incomingContinuation` (long-lived stream the delegate's `didReceiveVerificationRequest` yields into), and `sdkController` (retained to keep the delegate's SDK-side reference graph explicit + drive idempotency on `start()`).
  - `incomingRequests()` rewritten to stash a continuation on the FlowStore + clear it on `onTermination`, so `routeIncomingRequest` actually drives the chat-list / sidebar banner.
  - `confirmEmojiMatch` no longer synthesises `.verified` — it only calls `controller.approveVerification()`. The delegate's `didFinish` (via `routeSasFinished`) is the single source of truth for completion. Without this, a hypothetical UI bug surfacing a confirm button on `.requested` could "verify" the user without ever comparing emojis.
  - `cancel(requestID:reason:)` now yields the local `.cancelled(reason:)` *before* awaiting `controller.cancelVerification()` so the UI dismisses immediately even when the SDK round-trip is slow. The delegate's `didCancel` (via `routeSasCancelled`) handles partner-initiated cancels separately — local cancels clear the FlowStore entry so the delegate's later callback no-ops cleanly.
  - `SessionVerificationData.emojis` mapped to `[SasEmoji]` via `SessionVerificationEmoji.symbol()` / `description()` (both `()` calls — the SDK type is a class wrapping FFI handles). `.decimals` variant surfaces as `.cancelled(reason:)` because the spec mandates emoji SAS.
  - `AppDependencies` (iOS + Mac) gained `verificationService(for:)` mirroring `syncService(for:)` — caches `VerificationServiceLive` per `session.userID`. **Load-bearing**: every consumer in the app now goes through this cache so the FlowStore + registered SDK delegate are SHARED across the chat-list banner, per-bot ChatView banner, Settings → Device, post-login gate, and the Mac Help menu sheets. Without per-session caching, the inline `VerificationServiceLive(...)` constructions each got their own empty FlowStore + their own un-registered delegate (the original bug shape).
  - `MatronApp` and `MatronMacApp` added `.task { try? await dependencies.verificationService(for: session).start() }` on BOTH the verify-gate branch and the post-verify branch. Mirrors the existing `syncService.start()` wiring — the verify-with-other-device flow needs the delegate registered too (otherwise the post-login SAS hangs the same way).
  - All inline `VerificationServiceLive(provider:session:)` callsites in iOS `ChatListView`, iOS `PostLoginVerificationView`, Mac `MacChatListView`, Mac `MacPostLoginVerificationView`, Mac `MatronMacApp.verifyDeviceSheet`, and Mac `MatronMacApp.showRecoveryKeySheetBody` now route through `dependencies.verificationService(for: session)` (or `verificationCenter?.service` with the cached instance as the fallback — same identity).
  - **SDK API names verified against `matrix-rust-components-swift v26.04.01`** at `MatronShared/.build/checkouts/matrix-rust-components-swift/Sources/MatrixRustSDK/matrix_sdk_ffi.swift`: `setDelegate(delegate: SessionVerificationControllerDelegate?)` (line 12805), 7-method delegate protocol with `didReceiveVerificationRequest(details: SessionVerificationRequestDetails)` / `didAcceptVerificationRequest()` / `didStartSasVerification()` / `didReceiveVerificationData(data: SessionVerificationData)` / `didFail()` / `didCancel()` / `didFinish()` (line 44329). `SessionVerificationData.emojis(emojis: [SessionVerificationEmoji], indices: Data)` (line 37475). `SessionVerificationEmoji` is a class with `symbol()` / `description()` methods (line 13089). `SessionVerificationRequestDetails.deviceId` is non-optional `String` — adapter translates empty string to `nil` for the routing protocol's `String?` shape.
  - **Tests:**
    - `VerificationServiceLiveTests` — 11 tests (was 6). New: `test_confirmEmojiMatch_callsApprove_butDoesNotYieldVerifiedItself` (locks B1 fix), `test_routeSasData_thenRouteSasFinished_drivesReadyForEmojiThenVerified`, `test_routeSasCancelled_yieldsCancelled_andClears`, `test_routeSasFailed_yieldsCancelledWithFailReason_andClears`, `test_routeSasFinished_withNoActiveFlow_isNoOp`, `test_start_withoutClient_throwsNotConfigured`. Renamed the prior `test_confirmEmojiMatch_callsApprove_yieldsVerified_andClearsCache` to assert the *opposite* invariant — `.verified` synthesis is the bug.
    - New `LiveSessionVerificationDelegateTests` — 8 tests covering each SDK callback routing into the correct `routeSas…` entry point via a `FakeRouter` actor, plus the empty-deviceId-becomes-nil translation and the no-route-for-`didAcceptVerificationRequest` invariant. Uses `MatrixRustSDK.SessionVerificationController(noHandle: .init())` test seam to construct a delegate without a live SDK Client.
    - `AppDependenciesTests` (iOS) + `MacAppDependenciesTests` (Mac) — 2 tests each: `test_verificationService_isCached_perSession` (identity check on the shared cache) + `test_verificationService_isDistinct_perUser` (privacy guard — different sessions must not share the FlowStore).
  - **Plan deviations (vs the QA report's prescribed shape):**
    - QA report suggests storing the SDK controller "in `FlowStore` or a new `@unchecked Sendable` field." Stored on `FlowStore` as `sdkController` (already the actor that owns per-flow state) — keeps the SDK lifetime check (`hasSDKController()`) on the same actor as the rest of the flow bookkeeping rather than scattering it.
    - QA report suggests "construct a `LiveSessionVerificationDelegate` (already exists) — pass in the FlowStore actor and the controller reference so the delegate's callbacks can resolve which open `requestID` they apply to." Going through the FlowStore directly from the delegate would have leaked the actor type into the delegate file. Introduced an internal `SessionVerificationFlowRouting` protocol so the delegate's seam is testable and the FlowStore stays an implementation detail of the live service. The delegate doesn't need to know about request IDs at all — `routeIncomingRequest` is the only callback that has one (from the SDK), the rest apply to whatever `activeFlowID` was set when the open SAS continuation entered the flow.
    - QA report says `cancel` should "wait for `didCancel`." Inverted: locally yield `.cancelled(reason:)` first so the UI dismisses without waiting for the SDK round-trip, THEN await `controller.cancelVerification()`. Reason: the SDK doesn't reliably re-fire `didCancel` for cancellations we initiated locally (its source-of-truth for the user-facing reason is whatever string we pass in, not the delegate's argless callback). The delegate's `didCancel` is still wired so partner-side cancels reach the UI; the `routeSasCancelled` body no-ops cleanly because the FlowStore entry was already cleared by the local `cancel`.
    - `confirmEmojiMatch`, on the other hand, DOES wait for the delegate — the call only awaits `approveVerification`. The view's `onChange(of: state)` waits for the delegate's `.verified` push to dismiss. The asymmetry is correct: confirm requires SDK confirmation (security), cancel can be optimistic (UX).
  - **What stayed in scope but the report flagged:** B1 only. M3 (FlowStore overwrite on second tap) is left for the M3 task — `routeIncomingRequest` overwrites `activeFlowID` if a second request arrives while a first is pending. The seam to fix that lives entirely in `FlowStore.setActiveFlowID` / per-request continuation handling, which the M3 task can build on top of. Existing test surface won't churn.
  - SPM total: 206 (was 193, +13 — 11 in `VerificationServiceLiveTests` was 6 / +5; 8 new in `LiveSessionVerificationDelegateTests`; existing `DeviceVerificationRequestObserverTests` 3, `RecoveryKeyManagerTests` 3, `VerificationDTOsTests` 5, `VerificationServiceFakeTests` 8 unchanged — net +5+8 = +13 in VerificationTests; rest of SPM unchanged since other test bundles weren't touched). iOS scheme: 46 (was 44, +2 — `verificationService` cache identity tests). Mac scheme: 57 (was 55, +2 — same identity tests).
- [x] **Wave 2 expert-QA fix-ups — B2/M5 + M3 + M2** (2026-05-03). Three findings landed in one wave on top of B1 (`60e65ee`) + M4 (`d98c660`).
  - **B2/M5 — VerificationCenter + per-bot SasViewModel hoisted to `@State`.** Previously `let verificationCenter = VerificationCenter(service: deps.verificationService(for: session))` lived inside `body` on both `MatronApp` / `MatronMacApp`. Every `@State` mutation (`bootstrapDone`, `session`, `verifyDone`, the Mac host's `bootstrapError` / `showVerifyDeviceSheet` / `showRecoveryKeySheet`) re-ran body and produced a NEW center. The orphaned center's `start()` task kept running on the stale instance; the new center's `start()` was never called because `ChatListView.onAppear` is per-view-identity (not per-render). Banner went silent; sheet builders received an orphaned center. Fix: `@State private var verificationCenter: VerificationCenter?` + `.task(id: session.userID) { … center.start(); verificationCenter = center }` + `.onDisappear { verificationCenter?.stop(); verificationCenter = nil }` on both hosts. Same anti-pattern in:
    - `ChatView.verifyBotSheetBody` (iOS) / `MacChatView.verifyBotSheetBody` (Mac) — rebuilt `SasViewModel` + opened a fresh `startSAS` stream on every parent body re-eval. Switched to `.sheet(item: $verifyBotContext)` (was `.sheet(isPresented:)`) AND extracted the body into a private `VerifyBotSheet` / `MacVerifyBotSheet` struct that owns `@State private var viewModel: SasViewModel` initialised in `init` via `_viewModel = State(initialValue: …)`. SwiftUI's State semantics keep that VM stable across the parent's re-renders. The `.sheet(item:)` switch makes the sheet identity match the matrixID, so a second tap after dismiss rebuilds cleanly.
    - `PostLoginVerificationView.sasWithOtherDevice` destination (iOS) / `MacPostLoginVerificationView.sasWithOtherDevice` (Mac) — same inline-VM bug. Extracted to `SelfVerifySasDestination` / `MacSelfVerifySasDestination`.
    - Mac `MatronMacApp.verifyDeviceSheet` (Help → Verify This Device…) — extracted to `HelpMenuVerifyDeviceSheet`.
    - Did NOT touch `ChatListView.sasSheetContent` (iOS) / `MacChatListView`'s incoming-request SAS sheet — same anti-pattern lives there but it's outside Wave 2's QA scope. Flagging as a follow-up.
  - **M3 — `FlowStore.setContinuation` finishes the prior continuation.** When the user taps "Verify with another device", cancels the sheet, then taps it again, the second `startSAS` re-registers under the same `userID` cache key. The prior implementation silently overwrote the dict entry — the first stream's `for await new in stream` loop never terminated because the producer never finished it. Fix: `setContinuation` now finishes any prior continuation with `.cancelled(reason: "Replaced by new flow")` BEFORE inserting the new one. Companion fix in `register(controller:for:)`: returns the prior controller (now `@discardableResult`) so the `startSAS` caller can best-effort `cancelVerification()` on it (the SDK's session-verification controller is a singleton per session, so the prior + new wrapped instances usually point at the same SDK controller and the cancel is a no-op when the SDK is already idle; `try?` because a partner-side cancel that already finished the flow would throw). The B1 agent's note about M3 building on top of the FlowStore seam was accurate — no churn to external test surface.
  - **M2 — `isUserVerified(matrixID:)` is now tri-state.** New `UserVerificationResult` enum (`.verified` / `.unverified` / `.unknown`) in `VerificationDTOs.swift`. Live impl: `userIdentity(userId:fallbackToServer: false) == nil` now maps to `.unknown` (was `false`). The per-bot banner in iOS `ChatView` / Mac `MacChatView` renders ONLY when the result is `.unverified` — `.unknown` (cold-start: identity not yet in the local crypto store) keeps the banner hidden so it doesn't flash on every fresh chat open before sliding-sync warms up `/keys/query`. §7.5 trust posture preserved because the unknown branch re-evaluates on the next sync tick: switched the per-bot evaluation `.task` from `.task { … }` to `.task(id: viewModel.items.isEmpty) { … }` so it re-fires exactly once when the empty initial timeline transitions to populated (the cheapest signal we have for "sliding-sync delivered something — local crypto store probably has the user identity now"). `FakeVerificationService` widened: default for un-seeded users is now `.unknown` (was `false`); legacy `setUserVerified(_:for:)` Bool seam preserved (`true` → `.verified`, `false` → `.unverified`); new `setUserVerificationResult(_:for:)` exercises the `.unknown` arm. Host-app fakes (`ChatViewBindingTests` / `MacChatViewTests` / `DeviceSettingsViewTests` / `MacDeviceSettingsViewTests`) updated to match.
  - **Did NOT touch B1 SDK-delegate code (`60e65ee`)** as instructed — Wave 1's surface is unchanged. Did NOT touch B3/M1 Keychain (Wave 3) or any Minors (Wave 4). Did NOT bump SDK version. Did NOT change snapshot baselines. Did NOT open new PRs.
  - **Tests:**
    - `VerificationServiceLiveTests` — 13 (was 11, +2). New: `test_secondTap_cancelsPriorContinuation` (M3 — pins the `.cancelled(reason: "Replaced by new flow")` drain), `test_secondTap_secondContinuationStaysAlive` (M3 — companion guard against an over-eager fix that finishes both continuations).
    - `VerificationServiceFakeTests` — 8 (no net change — replaced `test_isUserVerified_defaultsToFalse_forUnknownUser` + `test_isUserVerified_returnsSeededValue` with 4 new: `test_isUserVerified_returnsUnknown_whenIdentityNotCached`, `test_isUserVerified_returnsVerified_whenIdentityVerified`, `test_isUserVerified_returnsUnverified_whenSeededFalse`, `test_setUserVerificationResult_drivesAllThreeStates`).
    - `ChatViewBindingTests` (iOS) — added `test_view_hidesBanner_whenBotVerificationIsUnknown` (M2 cold-start path) + `test_perBotSasSheet_callsStartSASExactlyOnce_perPresent` (B2/M5 structural baseline — ChatView constructor MUST NOT call startSAS).
    - `MacChatViewTests` — same two additions mirrored (`test_view_hidesBanner_whenBotVerificationIsUnknown`, `test_perBotSasSheet_callsStartSASExactlyOnce_perPresent`).
    - `AppDependenciesTests` (iOS) + `MacAppDependenciesTests` — added `test_verificationCenter_canBeBuilt_fromCachedService` each: VerificationCenter wraps the cached service identity (so every consumer shares a FlowStore + delegate); start/stop is idempotent.
  - **Plan deviations (vs Wave 2's prescribed shape):**
    - The QA spec mentioned exposing the center via Environment or a viewModel accessor for the binding test that "asserts the same VerificationCenter instance survives parent re-renders." That test requires SwiftUI rendering (we don't currently host views in XCTest), so the `@State` preservation is locked at the source level (`@State private var verificationCenter: VerificationCenter?` on both hosts) and structurally exercised via `test_verificationCenter_canBeBuilt_fromCachedService` + the existing `test_start_dedupesByRequestID` SPM idempotency test. Documented in inline comments on the host's verificationCenter property so the rationale survives a future refactor.
    - For the per-bot SAS sheet, the QA spec offered "switch to `.sheet(item:)` OR hoist VM into `@State`" — chose BOTH for defence-in-depth. `.sheet(item:)` controls when the sheet body fires; `@State` in the extracted `VerifyBotSheet` struct controls VM identity within that body. The combination means a regression that re-inlines the VM into the parent's `@ViewBuilder` would also have to re-introduce `.sheet(isPresented:)` to fully break — strictly stronger than either fix alone.
    - M3's "best-effort cancel the previous SDK controller" half is gated behind `try?` because a partner-side cancel that already finished the flow would otherwise throw and mask the new flow's setup. Per the QA spec: this is "best-effort also" — explicit non-fatal.
    - Did NOT update CI workflow / project.yml (no new SPM products, no new test target needed). Did NOT run `xcodegen` (project.yml unchanged).
  - SPM total: 210 (was 206, +4 — see test breakdown above). iOS scheme: 49 (was 46, +3). Mac scheme: 60 (was 57, +3).
- [x] **Wave 3 expert-QA fix-ups — B3 + M1** (2026-05-03). Two related Keychain hardening findings landed in one wave on top of Wave 2 (`9c3725a`).
  - **B3 — Pin Keychain access group explicitly on every `KeychainStore` construction.** Prior to this, `KeychainStore(service: "chat.matron.recovery", synchronizable: true)` was constructed inline at six call sites with `accessGroup: nil`. The system fell back to "first entry in `keychain-access-groups`" — fine today, broken Phase 4 (when iOS adds a second group for NSE push decryption). Items written today would land in whichever group the system happened to default to, and a future explicit `accessGroup:` argument wouldn't see them. Fix: new `KeychainAccessGroups.recovery` constant in `MatronShared/Sources/Storage/KeychainAccessGroups.swift` (`$(AppIdentifierPrefix)chat.matron.mac` on macOS, `$(AppIdentifierPrefix)chat.matron` on iOS — `#if os(macOS)` switch) + `KeychainStore.recoveryStore()` static factory that bundles service + group + `synchronizable: true`. Funnelled all six prod call sites through the factory: iOS `ChatListView.deviceSettingsSheetBody`, iOS `PostLoginVerificationView` (.generate + .restoreWithRecoveryKey), Mac `MatronMacApp.bootstrap()` (probe), Mac `MatronMacApp.showRecoveryKeySheetBody`, Mac `MacPostLoginVerificationView` (.generate + .restoreWithRecoveryKey). Only `KeychainAccessGroups.recovery` + `KeychainStore.recoveryStore()` know the strings now.
  - **M1 — iOS Keychain probe + entitlement (parity with Mac Task 13).** Phase 1 deferred iOS Keychain due to Sim entitlement issues; `RecoveryKeyManager` writes to a real synchronizable `KeychainStore` regardless. Real-device builds with broken entitlements would silently fail recovery-key persistence (caught by `RecoveryKeyManager.PersistenceError.keychainWriteFailedButKeyAvailable`'s catch — but only AFTER the user has signed in + generated a key). Mac has a setup-time probe; iOS now does too:
    - `Matron/App/Matron.entitlements` + `project.yml`: added `keychain-access-groups: [$(AppIdentifierPrefix)chat.matron]` (Phase-3-stable suffix; `chat.matron` not `chat.matron.app` so Phase 4's NSE share lands cleanly on the same group).
    - `MatronApp.bootstrap()`: gained a setup-time `KeychainProbe.run(keychain: KeychainStore.recoveryStore())` race-with-2s-timeout (mirroring Mac); `#if !targetEnvironment(simulator)`-gated so iOS Sim CI (which can't resolve `$(AppIdentifierPrefix)` without a signing team) stays green. New `@State private var bootstrapError: String?`; new `KeychainSetupErrorView` body branch when probe fails. Sentinel `KeychainProbeTimeout` mirrors the Mac private struct.
    - `KeychainSetupErrorView`: extracted from the Mac private `MacKeychainSetupErrorView` into `MatronShared/Sources/DesignSystem/KeychainSetupErrorView.swift` so both hosts mount the same view. `#if os(macOS)` keeps the Mac-side fixed `480×360` frame; iOS uses `.frame(maxWidth: .infinity, maxHeight: .infinity)` to fill the WindowGroup. Doc-path defaults to `docs/setup-mac.md` / `docs/setup-ios.md` per platform; caller can override.
  - **Tests:**
    - `RecoveryKeyManagerTests` — `test_recoveryStoreFactory_usesCentralisedAccessGroup` (B3 — pins `KeychainAccessGroups.recovery` suffix per platform via `hasSuffix("chat.matron.mac")` on macOS / `hasSuffix("chat.matron")` + NOT-`.mac` defence on iOS; live round-trip half is `XCTSkip`-gated behind `errSecMissingEntitlement -34018` since the SPM host lacks the iCloud-Keychain entitlement).
    - `KeychainProbeTests` — `test_run_usesRecoveryStoreFactory_forBootstrapParity` (M1 — pins the bootstrap probe routes through the SAME factory; gated XCTSkip on `errSecMissingEntitlement` for SPM-host coverage, structural wiring asserted regardless), `test_probeError_localizedDescriptions_areNonEmpty_forBootstrapHardGate` (negative-path: every `KeychainProbeError` branch produces a non-empty string suitable for the hard-gate `KeychainSetupErrorView` body — guards against a future refactor dropping `LocalizedError` and leaving the UI showing empty `Text(message)`).
  - **Plan deviations (vs the QA spec):**
    - QA spec said "factory `KeychainStore.recoveryStore(for: session)`." Implemented as `KeychainStore.recoveryStore()` (no session arg) — the access group is platform-only, the service string is fixed, and `synchronizable` is always `true` for the recovery-key path. Adding a `UserSession` arg would have introduced a new cross-target dep (`MatronStorage` → `MatronModels`) for no callsite benefit (every caller already passes the session into `RecoveryKeyManager(session: …)` one layer up). The factory's job is "encapsulate the service + access group" — the per-user account name lives on `RecoveryKeyManager.storageKey(for: userID)`, untouched.
    - QA spec said "extract `KeychainSetupErrorView` to `MatronShared/Sources/DesignSystem/` OR mirror it as `KeychainSetupErrorView` in iOS-app target." Chose extraction — Mac was the only existing implementation, the view is dependency-free (just SwiftUI + a `String`), and dedup is strictly better than parallel implementations that can drift. The Mac private `MacKeychainSetupErrorView` is gone; both hosts mount `KeychainSetupErrorView`.
    - Did NOT add a `KeychainSetupErrorView` snapshot test. The view is tiny + already covered structurally by the host-app body's UI test (when `bootstrapError` is non-nil, the host renders this view); a snapshot-pixel test would just churn baselines on the next renderer drift. The Mac suite already skips snapshots in CI via `MATRON_SKIP_SNAPSHOT_TESTS=1`.
    - **Live verification limitation:** the iOS probe needs a real signing team to actually exercise the access-group resolution (Sim can't, SPM can't, CI Sim can't). The structural fix (factory + probe wiring + entitlement file) IS landed and tested where possible, but the "does the probe actually pass on a real signed device build" half is deferred to manual TestFlight verification. The probe's `#if !targetEnvironment(simulator)` gate means CI iOS Sim runs are unaffected.
    - Did NOT touch B1 / B2 / M2 / M3 / M5 code (Wave 1+2 surface unchanged). Did NOT bump SDK version. Did NOT change snapshot baselines. Did NOT open new PRs. Did NOT change Mac probe behavior — just extended to iOS. Did NOT touch the Minors (Wave 4).
  - **xcodegen:** project.yml's iOS entitlements property changed; ran `xcodegen` to regenerate `Matron/App/Matron.entitlements`. Generated file matches checked-in.
  - SPM total: 213 (was 210, +3). iOS scheme: 49 (no change). Mac scheme: 60 (no change). 4 SPM tests skipped on hosts without iCloud-Keychain entitlement (was 2 — `AuthServiceLiveIntegrationTests` + `KeychainStoreTests.test_synchronizableInstance_writesIndependently`; +2 new on the same gate: `KeychainProbeTests.test_run_usesRecoveryStoreFactory_forBootstrapParity` + `RecoveryKeyManagerTests.test_recoveryStoreFactory_usesCentralisedAccessGroup`).
