[project]
name = "matron-test-partner"
version = "0.1.0"
description = "Scriptable Matrix partner client for Matron integration tests"
requires-python = ">=3.11"
dependencies = [
    "matrix-nio[e2e]>=0.25.0",
    "aiohttp>=3.9.0",
]

[tool.uv]
package = false
